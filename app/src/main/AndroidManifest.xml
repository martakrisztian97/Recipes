<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recipes">
        <activity
            android:name=".PizzaActivity"
            android:exported="true"
            android:label="@string/pizza"
            android:theme="@style/Theme.Pizza" />
        <activity
            android:name=".MuffinActivity"
            android:exported="true"
            android:label="@string/muffin"
            android:theme="@style/Theme.Muffin" />
        <activity
            android:name=".GumiSutiActivity"
            android:exported="true"
            android:label="@string/gumiSuti"
            android:theme="@style/Theme.GumiSuti" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>